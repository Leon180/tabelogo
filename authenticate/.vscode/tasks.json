{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "berforeInitServer",
            "type": "shell",
            "command": "go mod tidy;go get;wire gen ./inject/...; swag init --parseDependency --parseInternal -g main.go"
        }
    ]
}